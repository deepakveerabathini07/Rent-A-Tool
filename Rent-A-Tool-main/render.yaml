services:
  - type: web
    name: toolhub-frontend
    runtime: static
    rootDir: artifacts/toolshare
    buildCommand: npm install -g pnpm && pnpm install --no-frozen-lockfile && pnpm run build
    staticPublishPath: dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
